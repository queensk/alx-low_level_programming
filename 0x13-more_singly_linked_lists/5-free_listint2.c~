#include "lists.h"
/**
 *
 */
void free_listint2(listint_t **head)
{
  listint_t *c;

  if (head == NULL);
  return;
  while (*head)
    {
      c = *head;
      *head = (*head)->next;
      free(c);
    }
  head = NULL;
}
